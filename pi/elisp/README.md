# Pi elisp

This Pi package runs raw Emacs Lisp in the existing interactive Emacs
daemon. After one second, it opens Emacs's standard debugger; pressing `q`
aborts the evaluation and returns its backtrace to Pi.
